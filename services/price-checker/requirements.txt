# Browser automation
browser-use==0.1.48
langchain-openai>=0.3.0

# Database (direct asyncpg — no ORM needed for simple read/write)
asyncpg==0.30.0

# AWS SDK (SQS event parsing, S3 screenshot upload)
boto3>=1.35.0

# JSON logging
python-json-logger==2.0.7

# LLM observability — traces every agent step (LLM calls, tools, screenshots)
langfuse>=2.0.0
